fn main() -> int {
    assert(3 + 3 == 6)
    assert(3 - 3 == 0)
    assert(3 * 3 == 9)
    assert(3 / 3 == 1)
    assert(3 % 3 == 0)
    assert(3 == 3)
    assert(3 != 1)
    assert(3 > 1)
    assert(3 < 6)
    assert(3 >= 3)
    assert(3 <= 3)
    return 0
}
